spring:
  application:
    name: api-core
  datasource:
    url: jdbc:h2:mem:apicoredb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

server:
  port: 8080
